/* let loadingCanva;

function setup() {
  loadingCanva = createCanvas(windowWidth, windowHeight);
  loadingCanva.parent(document.querySelector(".loading"));
  loadingCanva.style("z-index", "101")
  
  angleMode(DEGREES);
  rectMode(CENTER);
}

function draw() {

} */